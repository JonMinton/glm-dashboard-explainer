Help add a new scenario to the GLM dashboard.

For a new scenario, we need:
1. Dataset (simulated or real) in `data/` folder
2. Scenario card definition (id, title, description, dataType, suggestedFamily)
3. Appropriate default transforms for this data type
4. Example R/Python code for follow-along exercises

Walk me through creating these components for the new scenario.

Usage: /add-scenario [scenario-name]
